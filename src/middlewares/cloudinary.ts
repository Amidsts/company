import { v2 } from "cloudinary";
import { NextFunction } from "express";

export function multipleImages(req, res, next: NextFunction) {
    let images = async (path, folder) => {

    }

    next()
}